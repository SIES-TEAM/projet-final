<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
    http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.1.xsd">

    <http auto-config="true"  >
        <intercept-url pattern="/user" access="hasAuthority('ADMIN')" />
        <intercept-url pattern="/user/add/user" access="isAuthenticated" />
        <intercept-url pattern="/person/add" access="isAuthenticated" />
        <intercept-url pattern="/person/delete" access="hasAuthority('ADMIN')" />
        <intercept-url pattern="/**" access="permitAll" />
        <form-login  login-page="/login"
                     username-parameter="username"
                     password-parameter="password"
                     authentication-failure-url="/accessDenied" />
        <csrf/>
    </http>

    <authentication-manager >
        <authentication-provider user-service-ref="customLoginService"/>
    </authentication-manager>

</beans:beans>